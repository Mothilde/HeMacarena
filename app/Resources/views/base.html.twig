<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}He Macarena{% endblock %}</title>
        {% block stylesheets %}
                <!-- Appel des bibliothèques Bootstrap -->
                <link rel="stylesheet" type="text/css" href="{{ asset('bundles/framework/css/bootstrap.css') }}" />
                <link rel="stylesheet" type="text/css" href="{{ asset('bundles/framework/css/bootstrap-theme.css') }}" />
                <!-- Appel de nos feuilles de style CSS -->
                <link rel="stylesheet" type="text/css" href="{{ asset('bundles/framework/css/style.css') }}" />
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
    <div class="navbar_homepage">
        <nav class="navbar navbar-inverse">
            <div class="container-fluid">
                <ul class="nav navbar-nav">

                    <li {% if app.request.get('_route') == 'fos_user_security_login' %}
                        class="active"{% endif %}>
                        <a href="{{ path('fos_user_security_login') }}">Connexion</a>
                    </li>

                    <li> <a href="#">Lien 3</a> </li>
                    <li> <a href="#">Lien 4</a> </li>
                    <li> <a href="#">Lien 5</a> </li>
                </ul>
            </div>
        </nav>
    </div>
    </body>
</html>
